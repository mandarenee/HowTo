<%= form_for(@how_to_topic) do |f| %>
  <% if @how_to_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how_to_topic.errors.count, "error") %> prohibited this how_to_topic from being saved:</h2>

      <ul>
      <% @how_to_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit "Create new How To" %>
  </div>
<% end %>
